# 绘忆 · 开发文档

> 移动端照片日记应用：把照片变成纸感立体绘本，把故事收进一本越来越厚的书。
> 本文档随开发进度持续更新，每版变更在文末「变更记录」登记。

---

## 1. 产品概述

**一句话定位**：一本由照片与故事组成的、会长厚的立体绘本日记。

**核心体验闭环**：

```
首页（文案缓缓跃动）
      │  拍照 / 从相册选一张
      ▼
本地算法立绘转换（≈1 秒，零等待）── 显著性起框 + GrabCut 抠图 + 剪纸分层 + 3D 预览
      │
      ▼
AI 引导对话（一次一问，2~4 轮，可随时主动收束）
      │
      ▼
日记生成（第二人称·回望式·150~400 字自适应）
      │
      ▼
收进书（插到书末）→ 下次打开书，直接落在最新一页，看到立体绘本与背后的故事
```

**差异化**：
- 「形」与「神」分离：**算法**做立体化（快、稳、零成本），**AI** 做理解与叙事（内容识别、引导、日记、记忆）。
- 立体绘本采用剪纸分层风——**抠图不完美不是 bug，而是纸的质感**。
- AI 有记忆：画像 + 近期日记注入，新对话能自然引用旧事。
- 一本书无限变厚——「岁月真的在变厚」是产品的记忆点。

---

## 2. 已确认的产品决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 形态 | 移动端 Web 应用（PWA），不局限平台、不上应用商店 |
| 2 | 立体化方式 | 确定性算法为主；**v0.7 修订：允许本地 ONNX 生成式风格化（AnimeGAN），分割/深度/补全仍为确定性或判别式模型**（生成式仅做整图风格迁移，不生成内容语义） |
| 3 | 主体识别 | 交给算法（后端 OpenCV）：显著性检测自动起框 + GrabCut 抠图（2026-08：由前端 WASM 迁至 server/） |
| 4 | 引导形态 | 文字微聊式：一次一个问题，2~4 轮，可主动收束；手机键盘自带语音转文字 |
| 5 | AI 记忆 | 结构化画像 + 最近 3~5 篇日记注入（向量检索等 ≥50 篇后再升级） |
| 6 | AI 理解照片 | 国内多模态 API（通义 VL / GLM-4V 等）：压缩至 512px 一次性理解，即用即弃 |
| 7 | 日记约束 | 第二人称「你」、回望式时态（「那天你……」）、150~400 字自适应、图文比例画面第一 |
| 8 | 书的信息架构 | 一本总书、按月分章、开书直接落在最新一页、3D 翻页动画 |
| 9 | 前端框架 | React + Vite + TypeScript + PWA + react-three-fiber（R3F）+ drei |
| 10 | 后端 | Python FastAPI「薄后端」：立绘转换（OpenCV）+ 密钥网关 + 云端档案（见 §5.4） |
| 11 | 部署节奏 | 前期本地开发 → 后期国内轻量云服务器（或香港轻量免备案） |
| 12 | 云端存储范围 | 不存原图；存：风格化立绘产物、AI 解读日志、画像档案（转换服务内存态处理、不留图） |
| 13 | 应用名 | **绘忆**（书默认名：《时光绘本》，可再调） |
| 14 | 设计方向 | 日式侘寂 Wabi-sabi（见 §4） |
| 15 | 工程原则 | 优先采用 GitHub 成熟开源方案，避免重复造轮子（见 §5.5 选型清单） |

---

## 3. 用户旅程与兜底行为

### 3.1 主流程

1. **首页**：留白为主，hero 区域文案缓慢跃动循环（备选文案见 §4.5），下方一个克制的「分享今天」入口（拍照 / 相册）。
2. **上传**：`<input type="file" accept="image/*">`（拍照加 `capture`）；先归一化 EXIF 方向再入流程。
3. **立绘转换（本地，约 1 秒）**：显著性起框 → GrabCut 抠图 → 剪纸分层（前景主体层 / 背景虚化层 / 纸页底）→ three.js 可交互预览（轻微倾斜视差）。
4. **AI 引导**：AI 基于照片解读以确认式提问开场（「画面里那只小家伙，是狗狗吧？」），一次一问；收到 VLM 解读后注入画像与近期日记；用户可随时点「写好啦」收束。
5. **生成日记**：AI 把照片元素与用户故事织成第二人称回望式短文，展示在绘本页下半部（画面第一）。
6. **收进书**：插入书末对应月份章节；提示「这一页已被收进你的书」。
7. **回看**：打开书 → 直接落在最新一页 → 3D 翻页浏览；点击单页可进入完整 3D 场景（实时交互）。

### 3.2 兜底行为（必须实现）

| 场景 | 兜底 |
|------|------|
| 断网 | 禁止引导对话，但允许先存照片草稿，联网后续传/继续 |
| 对话中途退出 | 对话存 IndexedDB，下次打开可继续 |
| 用户无话可说/跳过 | AI 从画面本身完成创作（不强迫对话） |
| VLM 误读画面 | 引导用确认式提问而非断言，用户可纠正 |
| AI 调用失败 | 可重试；连续失败则提示稍后再试，草稿不丢 |
| 浏览器缓存被清/换机 | 「全部导出」按钮（zip：原图 + 立绘 + 日记 JSON） |
| 云端档案删除 | 设置页提供「删除我所有云端档案」入口 |

---

## 4. 视觉与设计规范 · 日式侘寂 Wabi-sabi

> 素一点、旧一点、留白多一点，有种安静的时间感。
> 侘寂风是一种用自然材料、留白和不对称表达时间与不完美的视觉风格。
> 注意：不是「米白配宋体的固定模板」——所有 token 都要服务内容。

### 4.1 设计原则

- 从真实材料和摄影中提取自然、低刺激的色板
- 用留白放慢阅读节奏，但具体比例由内容决定
- 字体选择服务于内容，中英文分别检查字距与可读性
- 通过轻微不对称建立平衡，不让布局失去重心
- 纸张、陶土等肌理只作辅助，不能干扰正文

### 4.2 组成结构

| 要素 | 说明 |
|------|------|
| 纸感底色 Paper Base | 带细颗粒的米白宣纸感；纹理不得干扰文字 |
| 自然色层级 Earth & Ink | 从照片与材质提取低刺激色；正文与状态保留足够对比 |
| 衬线排版 Serif | 衬线字体 + 字距增强阅读节奏；中文正文以清晰易读为先 |
| 间 · 留白 Ma | 用空白控制节奏与关系，比例由内容和屏幕决定 |
| 不对称平衡 Asymmetry | 用内容、留白、对齐关系建立平衡，不强制居中 |
| 朱红点缀 Accent | 高饱和点缀承担落款与关键强调；控制用量 |

### 4.3 设计 Token（草案，随设计深化调整）

**色彩**：

| Token | 值 | 用途 |
|-------|-----|------|
| `--paper` | `#F4EFE6` | 纸感底色（米白，叠加 canvas 生成的颗粒噪点纹理） |
| `--ink` | `#3A362F` | 主墨色：正文、标题 |
| `--ink-soft` | `#857E70` | 半墨：次要文字、时间戳 |
| `--earth` | `#B49A7C` | 陶土：插图、分隔、选中态 |
| `--moss` | `#77806B` | 苔色（可选辅助土色）：状态、成功 |
| `--vermilion` | `#B83A1E` | 朱红：落款、关键强调（如收进书的一瞬）、hero 点睛 |

**字体**（全开源，自托管，woff2 子集化）：
- 正文（中）：思源宋体 `Noto Serif SC`
- 书封/书名/大标题（中）：霞鹜文楷 `LXGW WenKai`（手写的温度，绘本气质）
- UI 工具字（小标签、状态）：系统字体栈（清晰优先）
- 数字/英文点缀：`Cormorant Garamond`（书名页、页码，使用时克制）

**尺寸与间距**：以内容和屏幕决定；正文行高 1.9 起步；留白优先于装饰。

**动效**：
- 全局弹簧缓动（iOS 手感）：`cubic-bezier(0.32, 0.72, 0, 1)`；急停/回弹场景用双段曲线
- 页面过渡：View Transitions API（Safari 18.2+ 已支持）
- 翻页：跟随手指位移，松手按速度/位移判定回弹或翻完
- 尊重 `prefers-reduced-motion`：减弱为淡入淡出

**签名元素**：剪纸分层立体绘本——每一页都是「立起来」的纸，配朱红小落款（日期 + 一句引语），整本书是唯一记得住的东西；其余保持安静。

### 4.4 3D 绘本与侘寂的呼应

- 剪纸分层本身就是纸的肌理，天然贴合侘寂
- 书封面：亚麻布纹 + 书名字体压印（canvas 程序化生成纹理，不引入外部素材）
- 纸页底色沿用 `--paper`，绘本背景层虚化时向纸色过渡

### 4.5 Hero 文案（缓慢跃动循环）

```
「今天，想和我分享些什么？」
「今天过得怎么样？」
「有没有一张照片，值得收进书里？」
```

约 12s 一轮：逐句淡入上浮 → 停留 → 淡出；实现为 CSS 动画，文案后续可配。

---

## 5. 技术架构

### 5.1 总体架构

```
┌──────────────────────────────┐       压缩图/对话/档案       ┌────────────────────────┐
│  PWA 前端（React + R3F）      │ ──────────────────────────▶ │ FastAPI 薄后端          │
│  ─ IndexedDB：原图/草稿/书/    │ ◀────────────────────────── │  · 密钥网关（AI 调用）    │
│    对话/画像（运行主库，离线可用）│                             │  · 云端档案（SQLite）：   │
│  ─ 本地算法管线：opencv.js     │                             │    立绘产物/解读日志/画像  │
│    （显著性+GrabCut+分层）      │                             │  · 限流 + 设备 token      │
│  ─ three.js / R3F 书与翻页     │                             └───────────┬────────────┘
└──────────────────────────────┘                                         │
                                                              ┌─────────▼─────────┐
                                                              │ 通义VL / GLM-4V 等  │
                                                              │（理解+对话+日记+画像）│
                                                              └───────────────────┘
```

### 5.2 前端栈

React 18 + Vite + TypeScript + PWA（workbox 壳缓存）；three.js + R3F + drei；opencv.js（WASM，按需懒加载，仅拍摄后拉取一次）；Zustand（状态）；IndexedDB（Dexie 封装）。

### 5.3 前端存储（IndexedDB 主要表，dev 期可调）

- `drafts`：待处理草稿（照片、上下文）
- `conversations`：引导对话记录（可续聊）
- `pages`：书页（立绘配置 JSON、图层图片 Blob、日记文本、时间、月份）
- `profile`：用户画像（结构化）
- `settings`：设备 token、偏好

### 5.4 后端（FastAPI，本地 uvicorn → 轻量云）

**职责边界**：不存原图、不做业务逻辑；只做 ① 立绘转换（OpenCV 管线，`server/`，v0.4 已实现）② AI 调用转发（持密钥）③ 云端档案持久化 ④ 限流。

**已实现**（v0.4，见 `server/`）：
- `POST /api/convert`：接收照片（≤25MB）→ 内存态 OpenCV 管线（EXIF 归一化 → ≤768 降采样 → 显著性起框 → GrabCut → 剪纸分层）→ 四层 PNG + 配置 JSON。处理完即弃，不落盘、不留图。
- 前端经 `web/src/features/convert/api.ts` 调用（`VITE_CONVERT_API` 可覆盖地址）；低端设备零 WASM 负担。

**端点草案**（dev 期可调整）：
- `POST /api/understand`：接收 512px 压缩图 → 调 VLM → 返回结构化照片解读（主体/背景/氛围/元素）
- `POST /api/chat`：引导对话（带画像 + 近期日记注入，服务端统一拼上下文，控制 token 预算）
- `POST /api/diary`：生成日记（并触发画像更新）
- `POST/GET /api/archive`：立绘产物（剪纸图层 PNG + 配置 + 缩略图）、解读日志、画像的云端副本
- `DELETE /api/archive`：删除云端档案

**存储**：SQLite（单文件，零运维）；画像存 JSON 字段；限流用内存级计数即可。

**密钥**：只存环境变量；前端永不可见。

### 5.5 开源依赖与选型清单（开源优先，避免造轮子）

**已定**：three.js / R3F / drei · opencv-python（server/，前端不再引入 opencv.js）· Workbox · Dexie · FastAPI · 霞鹜文楷 / Noto Serif SC（开源字体）。

**待验证（进入 M1 时先去 GitHub 确认是否可用/可裁剪）**：
- [tiefling](https://github.com/combatwombat/tiefling)：2D→3D 图像生成器与查看器，验证立绘链路参考价值
- [DepthParallax](https://github.com/giulioz/DepthParallax)：深度图视差（three.js）
- [three-layered-material](https://github.com/aiira-co/three-layered-material)：多层照片材质
- [Depthflow-WebGL](https://github.com/akatz-ai/Depthflow-WebGL)：照片 3D 化参考
- 3D 翻页组件（three.js 版翻书库，如 PageFlip 系）——选定后替换手写方案
- opencv.js 显著性/抠图实现路径

**原则**：能整体引入就整体引入（保留取舍余地：需求 80% 可覆盖且体积可控时直接使用）；裁剪或自研必须记录理由于本文档。

### 5.6 项目目录结构

```
huiyi/                        ← 仓库根（建议当前目录更名为 huiyi）
├── development.md            # 开发文档（随进度更新）
├── README.md                 # 启动说明、环境变量、一键起前后端
├── .gitignore                # node_modules / dist / server/data / .env / __pycache__
│
├── web/                      # ── 前端：React + Vite + PWA ──────────
│   ├── index.html
│   ├── vite.config.ts
│   ├── public/
│   │   ├── icons/            # PWA 图标 + manifest
│   │   ├── fonts/            # 字体 woff2 子集（霞鹜文楷全量约 10MB，必须子集化）
│   │   └── wasm/             # opencv.js（懒加载，进转换页才拉）
│   └── src/
│       ├── pages/            # 路由壳，只管布局和导航
│       │   ├── home/         # 首页 hero + 上传入口
│       │   ├── convert/      # 立绘转换与交互预览
│       │   ├── story/        # AI 引导对话
│       │   ├── entry/        # 日记生成展示（绘本页预览）
│       │   └── book/         # 书：封面 / 月章 / 翻页浏览 / 单页 3D
│       ├── features/         # 领域模块，自包含、互不渗透
│       │   ├── pipeline/     # 立绘管线（opencv 封装 + Web Worker）
│       │   ├── ai/           # 后端 API 客户端（understand/chat/diary）
│       │   ├── memory/       # 画像管理与注入（§8 落地处）
│       │   └── export/       # 导出 zip 逃生口
│       ├── three/            # R3F 场景组件：书、翻页、2.5D 立绘场景
│       ├── db/               # Dexie schema + 数据仓库（对应 §5.3）
│       ├── design/           # 设计 token、弹簧曲线、纸纹理生成器
│       ├── components/ hooks/ lib/ styles/
│       └── main.tsx router.tsx providers.tsx
│
├── server/                   # ── 后端：FastAPI ─────────────────────
│   ├── requirements.txt
│   ├── .env.example          # 密钥占位（.env 不入库）
│   ├── app/
│   │   ├── main.py           # 入口 + CORS
│   │   ├── config.py         # 环境变量加载
│   │   ├── routers/          # 薄路由：understand / chat / diary / archive
│   │   ├── llm/              # 核心资产：视觉理解、对话、日记、画像提取 prompt
│   │   │   └── profile.py    # 画像增量合并算法
│   │   ├── db/               # SQLite：模型 + 档案仓库
│   │   └── middleware/       # 限流 + 设备 token
│   ├── tests/
│   └── data/                 # SQLite 文件（gitignore）
│
├── docs/                     # 架构图、API 契约（共享 DTO 定义）
└── scripts/                  # 本地一键起、字体子集化、部署脚本
```

**结构原则**：
- 同仓库、不用 pnpm workspaces（前后端无共享代码，只经 HTTP + 约定 DTO 通信）；一次提交 = 一次进度
- `features/` 是领域切片自包含，`pages/` 只是路由壳；删除功能 = 删除目录，不牵连他处
- 立绘管线必须跑在 Web Worker（opencv.js 数百毫秒，不能卡主线程）
- 字体必须子集化（霞鹜文楷全量约 10MB → 2~3MB），且 WASM 与字体纳入 PWA 缓存（离线也能转换）
- 前后端 DTO 契约放 `docs/`，避免字段各写各的

---

## 6. 移动端兼容与性能

### 6.1 iOS Safari 硬约束（最容易翻车）

- 不支持 File System Access API → 一律用 `<input type="file" accept="image/*">`，拍照加 `capture`
- 照片 EXIF 方向必须在转换前归一化
- opencv.js WASM 注意按需加载与内存峰值
- WebGL 可用但低端机需降级

### 6.2 性能策略

- **书页两层渲染**：书页浏览 = 预渲染立绘图 + 轻量视差（CSS/低开销 WebGL）；点进单页才加载完整 3D 场景（进入时构建、退出时释放）
- 纹理全部降采样至屏幕分辨率；离屏渲染缓存立绘产物
- 后端注入的旧日记每篇截断（如 300 字），控制 token 与请求体积
- 立绘转换在 512px 工作图上进行

### 6.3 降级路线

低端机 / `prefers-reduced-motion` / WebGL 不可用 → CSS 视差版书页；立绘转换失败 → 提示重试，草稿保留。

---

## 7. 合规与隐私

- **备案**：国内轻量云 + 域名上线需 ICP 备案（1~3 周）；开发期本地即可；如想免等待可选香港/新加坡轻量服务器
- **隐私说明页**：说明照片本地存储、压缩图仅用于一次性理解、云端仅存立绘产物/日志/画像
- **删除入口**：设置页一键删除全部云端档案
- **内容安全**：依赖国内多模态 API 自带审核；违规结果低调拦截，不展示
- 设备 token 简单鉴权（单设备原型），不引入账号体系

---

## 8. 记忆机制（画像）

### 8.1 总体分层（长短期记忆分工）

| 层 | 内容 | 寿命 | 何时启用 |
|----|------|------|----------|
| L0 对话内 | 本轮引导对话全文 | 单次会话 | MVP 即用 |
| L1 短期 | 最近 3~5 篇日记（每篇截断 300 字） | 数周 | MVP 即用 |
| L2 长期·画像 | 结构化画像 JSON（人物/地点/偏好/重要日期/叙述习惯…） | **永久**，跨会话跨天 | MVP 即用 —— 这就是「长期画像记忆」 |
| L3 长期·情境 | 全量日记向量检索（RAG） | 永久 | ≥50 篇后评估上 |
| L4 演化 | 月度画像快照（画像随人成长的历史） | 永久 | 可选，后期 |

### 8.2 画像的数据模型

画像是一个 JSON 文档（前端 IndexedDB + 后端 SQLite 各一份副本），字段草案：

```jsonc
{
  "user": { "selfLabel": "我", "naming": "怎么称呼用户" },
  "people": [   // 每项：名称、关系、特质、出现次数、最近出现
    { "name": "妈妈", "relation": "母亲",
      "traits": ["做菜很好吃，拿手菜是红烧肉"],
      "count": 12, "lastSeen": "2026-08-25" }
  ],
  "places": [ { "name": "外婆家的老院子", "meaning": "童年暑假的记忆", "count": 3, "lastSeen": "…" } ],
  "preferences": [ { "topic": "雨天", "note": "喜欢雨后的空气，尤其是夏天", "count": 5 } ],
  "importantDates": [ { "date": "2026-06-01", "note": "搬家到新城市的第一天" } ],
  "habits": [ { "topic": "叙述习惯", "note": "讲故事时喜欢先交代天气" } ],
  "updatedAt": "2026-08-25T12:00:00Z"
}
```

### 8.3 哪些信息算「关键」？—— 提取判定标准（写在提取 prompt 里）

只提取**可复用的稳定事实**，过滤一次性事件。判定五问（满足其一即关键）：

1. 未来对话**还用得上**吗？（偏好、人物关系、地点意义 → 用得上；「昨天路过的咖啡店」→ 进日记即可）
2. 是**跨时间稳定**的吗？（「妈妈做菜很好吃」稳定 → 关键；「今天下雨」一次性 → 不关键）
3. 能**影响引导质量**吗？（「你说过怕狗」能改变提问方向 → 关键）
4. 是**情感锚点**吗？（重要日期、长期愿望 → 关键）
5. 是**叙述习惯**吗？（口头禅、叙事偏好 → 关键，用于模仿文风）

**明确不存**：一次性事件细节（归日记）、敏感信息（健康、财务、精确住址）。

### 8.4 提取与合并（工程关键：确定性合并，而非让 LLM 全量重写）

- **提取时机（零成本技巧）**：日记生成时让模型**同一次调用**输出「日记 + 候选画像更新」两份结构化字段——不为记忆多花一次 API 调用。
- **合并规则（代码写死，不用 LLM）**：
  - 同实体（按 name/topic 归一化匹配）→ 合并：更新 `count+1`、`lastSeen`、互补 traits（保留并集，按 count 排序）
  - 新实体 → 追加
  - 冲突描述 → 保留 count 更高者，低者降级为备注
  - 画像膨胀上限（如 200 项）→ 按 count/recency 裁剪
- **注入格式**：画像渲染成自然语言段落（比 JSON 省 token、表达力强），如：「关于 TA 的记忆：妈妈（做菜很好吃，拿手菜是红烧肉；最近 3 次提到）……」+ 最近日记截断，随对话请求发给模型。

### 8.5 实现难度评估

| 部分 | 难度 | 说明 |
|------|------|------|
| 画像存储（JSON 双副本） | ★ | 无状态设计，两天内稳 |
| 提取 prompt（五问判定） | ★★ | 迭代 2~3 次即可稳定；和日记共用一次调用，零额外成本 |
| 确定性合并算法 | ★★ | 去重/冲突/裁剪是纯代码逻辑，写清楚测试即可 |
| 注入与 token 预算 | ★ | 截断 + 渲染成自然语言段落 |
| 验收（「AI 记得我」） | ★★ | 用 5 条固定测试记忆点验证新对话能否自然引用 |

**总体：★★☆（中等偏易），预计 1 周内完成核心。** 注意：画像消费方（对话引导）在 M2 就要用，所以画像的提取/合并设计跟随 M2 一起做，M4 只做验收强化与调优。

---

## 9. MVP 范围

> 状态标记（v0.3 验收）：✅ 已完成 · 🟡 完成但有已知偏差（详见 v0.3 变更记录）

### 做（核心闭环）

1. ✅ 首页：hero 文案缓缓跃动 + 拍照/相册入口
2. ✅ 本地立绘转换：显著性 + GrabCut + 剪纸分层 + three.js 交互预览（工作图 ≤1024、opencv.js 懒加载，见 docs/pipeline.md）
3. ✅ AI 引导对话：一次一问、2~4 轮、可主动收束；断网禁用、对话落 IndexedDB 可续聊（Mock 适配器顶替后端）
4. ✅ 日记生成：第二人称回望式、150~400 字自适应、绘本页下半部（Mock 适配器模板拼装）
5. ✅ 收进书 + 3D 翻页：单本总书、按月分章、开书落最新页、跟手翻页（选型与成本见 docs/book-3d.md）
6. ✅ 画像记忆：增量更新 + 对话注入（features/memory；Mock 期关键词启发式顶替 LLM 提取）
7. 🟡 薄后端：本轮纯前端未实现；前端已按 docs/api-contract.md 的 DTO 形状接 Mock 适配器，设置页「删除云端档案」当前清除本地副本，接后端后对齐 DELETE /api/archive
8. ✅ PWA 壳：可装主屏、离线可翻书（workbox 壳缓存；字体/opencv 走运行时 CacheFirst）
9. ✅ 「全部导出」zip（数据逃生口：原图 + 立绘图层 + 日记 JSON + 对话 + 画像）

### 先不做（验证核心功能后再补）

账号登录、多设备同步、分享导出、日记编辑（仅保留长按删除）、深色模式、通知、统计、向量检索记忆。

---

## 10. 里程碑

| 里程碑 | 内容 | 完成标准 |
|--------|------|----------|
| M1 | 本地立绘管线 | 选一张照片 → 1 秒内出剪纸分层立绘 + 可交互预览；验证 GitHub 开源方案（tiefling 等）并落定取舍 |
| M2 | 引导对话 + 日记生成 | 确认式提问、2~4 轮收束、生成 150~400 字第二人称回望式日记 |
| M3 | 书 + 翻页 | 单本总书、月章、开书落最新页、3D 翻页流畅（含低端机降级） |
| M4 | 画像记忆 | 日记后画像增量更新；新对话自然引用旧事 |
| M5 | PWA 壳 + 兜底补全 | 可安装、离线翻书、断网草稿、导出 zip、删除云端档案、隐私页 |

每步单独可验证；M1 验证通过前不进入 M2。

---

## 11. 已知风险清单

| 风险 | 缓解 |
|------|------|
| VLM 误读照片内容 | 确认式提问，允许用户纠正 |
| 转换后端不可用（网络/服务故障） | 转换页明确报错 + 重试；书与草稿不受影响；部署后加健康检查与告警 |
| 转换需上传照片的隐私感知 | 隐私说明明示「处理完即弃、不存原图」；原图始终留本机 |
| 上下文体积膨胀 | 注入截断 + token 预算管理 |
| 本地数据丢失 | 导出 zip 逃生口 |
| 云端档案合规 | 隐私说明 + 删除入口 + API 自带审核 |
| 备案周期 | 香港轻量备选方案 |

---

## 12. 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-08-25 | 立项：产品定位、全部产品决策（决策表 §2）、侘寂设计规范草案、技术架构、MVP 范围与里程碑 |
| v0.2 | 2026-08-25 | 新增 §5.6 项目目录结构；深化 §8 记忆机制：长短期分层、画像数据模型、关键信息判定五问、零成本提取与确定性合并、难度评估 |
| v0.3 | 2026-08-26 | **前端 MVP 完成（T1~T9）**：工程骨架（Vite+React+TS+PWA+Dexie+Mock AI）→ 侘寂设计系统与首页 hero → 本地立绘管线（opencv.js + Web Worker：显著性→GrabCut→剪纸分层，docs/pipeline.md）→ R3F 3D 场景（立绘交互预览 + 3D 书跟手翻页，docs/book-3d.md）→ 主流程页面串联（上传→转换→对话→日记→收书）→ 书浏览（封面/月章/翻页/单页3D/长按删除）→ 画像记忆 → PWA 壳 + 导出 zip + 设置/隐私页 + 断网兜底 → 集成验收与文档。§9 完成项已打勾。<br>**已知偏差**：① AI 全程 Mock 适配器（understand/chat/diary/画像提取），后端未实现，DTO 见 docs/api-contract.md；② 书空表时以演示数据兜底（`features/book/data.ts` 的 `BOOK_MOCK_FALLBACK`，验收后可置 false）；③ opencv.js 10MB 懒加载 chunk 首次转换需拉取（之后 CacheFirst 离线可用），warm 转换约 <1s（docs/pipeline.md §4 timingMs）；④ 3D 翻页松手速度阈值（0.45 页/秒）与视口手感待真机复核；⑤ 容器 16/10+maxHeight 双约束在横屏下比例失真（manifest 已锁竖屏，低优先）。<br>**遗留项**：T9 验收清单见 docs/t9-checklist.md；后端接入（server/）；画像提取换真 LLM；向量检索（≥50 篇后）；字体子集化脚本（scripts/）。 |
| v0.4 | 2026-08-26 | **立绘转换迁至 Python 后端（server/）**：技术路线由「前端 opencv.js + Web Worker」调整为「FastAPI + OpenCV」（低端设备友好、速度稳定、版本统一）。<br>① 后端：`server/app.py`（POST /api/convert，≤25MB，内存态处理不留图）+ `server/pipeline.py`（算法与 TS 版一一对应：EXIF 归一化 → ≤768 降采样 → 频谱残差显著性 → GrabCut(2 迭代) → 剪纸分层[纸缘/淡墨线/纸纹颗粒/投影] → 配置 JSON），热运行约 0.5s/张；② 前端：`features/convert/api.ts` 替换本地管线，删除 opencv.js/WASM/Worker 全部代码与 10MB chunk（包体显著变小），`VITE_CONVERT_API` 可配后端地址；③ 修复：背景层 R/B 通道写反（"背景颜色不对"根因）、WebGL 运行期崩溃整页白屏（GLBoundary 静态降级）、PWA 旧 SW 缓存窗口（controllerchange 自动刷新）、转换卡死永久转圈（任务超时+崩溃监听，后随迁移自然下线）；④ 提速：工作尺寸 1024→768、GrabCut 4→2 迭代、背景模糊半分辨率（本地版本实测 3.0s→1.7s，后端 0.5s）；⑤ 隐私文案更新（转换页/设置页：照片发送至转换服务器、处理完即弃、原图留本机）。<br>**已知偏差**：① AI 仍为 Mock 适配器；② 转换与 AI 对话需网络（离线可翻书/读写草稿）；③ 后端鉴权/限流待部署时接入（§5.4）。 |
| v0.7 | 2026-08-26 | **立绘 v4：生成式风格化 + 更强分割（双模型并行）**。<br>① 风格化升级（`server/stylize.py` v3）：AnimeGANv2（**Hayao 宫崎骏风**，8.6MB ONNX 本地推理 0.5s，face_paint 兜底）→ 侘寂调色/纸混合/颗粒后处理（`animegan-wabi`）；模型缺失自动回退确定性链（调色+磨平+量化+纸纹）；② 分割双档（`server/multilayer.py` + 新增 `server/sam.py`）：默认 FastSAM（~2s，带裁剪修复/碎片合并/GrabCut 精修），`HUIYI_SEGMENTOR=sam` 切 SAM vit-base 量化（106MB，mask 完整锐利，~10s）；③ 管线重构：**分割在原图上执行、风格化独立并行**（互不依赖，ThreadPool 并行 + onnx 会话限 4 线程防 CPU 争抢），FastSAM+AnimeGAN 实测总耗时 **3.0s**；④ 分割→风格化→深度分层三段解耦：mask 来自原图，像素来自风格化图，背景 inpaint 填充。⑤ 模型清单：FastSAM_S 47MB + DAv2 27MB + AnimeGAN 8.6MB + SAM 106MB（质量档），共 ~190MB，首次使用自动下载。<br>**已知偏差**：① SAM 质量档 ~10s（Intel CPU 编码器瓶颈，Apple Silicon 会快）；② AnimeGAN 输出偏日漫，靠侘寂后处理收敛到纸感（风格后续可换 Hayao/Shinkai 权重）；③ AI 仍为 Mock。 |
| v0.6 | 2026-08-26 | **立绘质量升级（三步管线落地）**：分割 → 补全/风格化 → 深度合成。<br>① 分割精修（`server/multilayer.py`）：FastSAM 粗 mask 经闭运算合并碎块 + 最大连通域（保留 ≥30% 卫星碎块）+ **GrabCut(mask 先验) 边界精修**（仅粗糙实例，roughness>1.45 触发）+ 边界平滑——"图层破碎/锯齿"修复（实测 5 碎块→2）；② 背景填充（`server/pipeline.py`）：元素区域先 `cv2.inpaint`（Telea）抹除残留再模糊纸化；③ 风格化 v2（`server/stylize.py`）：在调色+磨平基础上新增**色阶量化 posterize**（14 级 + 抖动噪声防色带，版画/水彩色块感）与**纸纹理 soft 混合**（30%，"印在纸上"质感），强度整体上调至可见的插画纸感；④ 实测：多元素路径 3.9s/张（FastSAM 0.8s + 深度 0.6s + 精修 1.3s + inpaint 0.2s + 风格化 0.14s），单主体回退 0.95s。<br>**已知偏差**：① 帧边截断元素的"语义级补全"需生成式模型（决策 #2 不引入），现按剪纸纸缘处理（干净截断）；② 多元素总耗时 ~4s，后续可对精修做并行或分级；③ AI 仍为 Mock。 |
| v0.5 | 2026-08-26 | **立绘升级：侘寂纸感风格化 + 多元素前中后分层**（决策 #2/#3 深化，均为确定性后处理 + ONNX 推理，非生成式）。<br>① 风格化（`server/stylize.py`）：侘寂调色 LUT（阴影抬升/暖米偏移/去饱和）→ 半分辨率 bilateral 磨平（耗时 4.8s→0.07s）→ 纸纹颗粒+纤维条带，全图统一（前/中/远/背景/纸底均继承）；② 多元素分层（`server/multilayer.py`）：FastSAM_S（47MB ONNX，实例分割）→ Depth-Anything-v2-small 量化（27MB，深度排序）→ 实例按中位深度分前/中/远三带，每带独立剪纸层；模型缺失/失败自动回退单主体（显著性+GrabCut）；模型自动下载（GitHub 镜像列表 + HF 镜像，`HUIYI_MODELS_DIR`/`HUIYI_GITHUB_MIRROR` 可配）；③ 前端：`IllustrationLayers` 增 midground/backdrop，3D 场景前/中/远带视差幅度 5%/3%/1.5%，静态降级同步；`CutoutConfig` 增 `layers[]`/`style` 元信息；④ 实测：多元素路径 1.9~2.3s/张（FastSAM 0.8s + 深度 0.6s），单主体回退 0.6s；脏边问题从根上消除（实例 mask 锐利二值化 + 形态学精修）；⑤ 环境：venv 固定 Python 3.12（onnxruntime 无 3.14 wheel）。<br>**已知偏差**：① FastSAM 对抽象/低对比场景可能漏检 → 自动回退；② 首次转换需下载 ~75MB 模型（离线部署预置 models/）；③ AI 仍为 Mock；④ 深度排序对摆拍多主体（近大远小）效果最佳。 |